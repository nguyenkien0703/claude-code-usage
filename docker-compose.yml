services:
  app:
    build: .
    container_name: claude-dashboard
    restart: unless-stopped
    ports:
      - "4455:4455"
    volumes:
      - ./sessions:/app/sessions
      - ./data:/app/data
      - ./.env:/app/.env:ro
